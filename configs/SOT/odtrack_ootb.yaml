# ODTrack (AAAI 2024) evaluation on OOTB — Oriented Object Tracking Benchmark.
# Usage: python eval_odtrack.py --config configs/SOT/odtrack_ootb.yaml
#
# OBB handling: see configs/SOT/ostrack_ootb.yaml — identical protocol (OOTB v1.0).

dataset: OOTB
dataset_root: /data/ESA_DLSTEM_2025/data/trafic/OOTB
class_map:
  car: 0
  plane: 1
  ship: 2
  train: 3

eval_mode: sot
obb_eval_mode: ootb_aabb
img_size: null

variant: baseline_large
yaml_path: /home/ziwen/code/esa_dlstem/ODTrack/experiments/odtrack/baseline_large.yaml
ckpt_path: /work/ziwen/checkpoints/odtrack/baseline_large/ODTrack_ep0300.pth.tar

prompt_strategy: first_frame
prompt_interval: 10

clip_len: 32
clip_stride: 1
batch_size: 1
num_workers: 0

iou_thresh: 0.3
score_thresh: 0.5
max_wandb_images: 50

wandb_project: esa-dlstem
wandb_entity: chengziwen693
