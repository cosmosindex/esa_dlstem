# LoRAT (ECCV 2024) evaluation on SatSOT — single-object tracking, HBB.
# Usage: python eval_lorat.py --config configs/lorat_satsot.yaml

dataset: SatSOT
dataset_root: /data/ESA_DLSTEM_2025/data/trafic/SatSOT
class_map:
  car: 0
  plane: 1
  ship: 2
  train: 3

eval_mode: sot
img_size: null

# LoRAT variant: g-378 = ViT-g/14 DINOv2 backbone, 378×378 search region (largest / best).
variant: g-378
lorat_root: /home/ziwen/code/esa_dlstem/LoRAT
method_name: LoRAT
ckpt_path: /work/ziwen/checkpoints/lorat/lorat_g378.bin

prompt_strategy: first_frame
prompt_interval: 10

clip_len: 32
clip_stride: 1
batch_size: 1
num_workers: 0

iou_thresh: 0.3
score_thresh: 0.5
max_wandb_images: 50

wandb_project: esa-dlstem
wandb_entity: chengziwen693
