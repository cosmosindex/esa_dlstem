# SAMURAI evaluation on SatSOT (satellite video single-object tracking, HBB)
# Usage: python eval_samurai.py --config configs/samurai_satsot.yaml

dataset: SatSOT
dataset_root: /data/ESA_DLSTEM_2025/data/trafic/SatSOT
class_map:
  car: 0
  plane: 1
  ship: 2
  train: 3

eval_mode: sot
img_size: null

model_name: large
ckpt_path: null

prompt_strategy: first_frame
prompt_interval: 10

clip_len: 32
clip_stride: 1
batch_size: 1
num_workers: 0

iou_thresh: 0.3
score_thresh: 0.5
max_wandb_images: 50

wandb_project: esa-dlstem
wandb_entity: chengziwen693
